{% comment %}
  Unified skills entry renderer for both formats - Two column grid layout
{% endcomment %}

<div class="row">
  {% for entry in entries %}
    <div class="col-md-6">
      <div class="skill-item">
        {% if entry.icon %}
          <i class="{{ entry.icon }}"></i>
        {% endif %}
        <strong class="skill-title">{{ entry.name }}</strong>
        {% if entry.keywords %}
          <ul class="skill-keywords">
            {% assign keywords_array = entry.keywords | split: ", " %}
            {% if keywords_array.size > 1 %}
              {% for keyword in keywords_array %}
                <li>{{ keyword }}</li>
              {% endfor %}
            {% else %}
              {% for keyword in entry.keywords %}
                <li>{{ keyword }}</li>
              {% endfor %}
            {% endif %}
          </ul>
        {% endif %}
      </div>
    </div>
  {% endfor %}
</div>

<style>
  .skill-item {
    padding: 0.5rem 0;
  }
  .skill-title {
    color: var(--global-theme-color);
    display: block;
    margin-bottom: 0.25rem;
  }
  .skill-keywords {
    list-style: none;
    padding-left: 0;
    margin: 0;
  }
  .skill-keywords li {
    padding: 0.1rem 0;
  }
</style>
